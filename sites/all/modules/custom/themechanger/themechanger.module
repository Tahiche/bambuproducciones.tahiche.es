<?php

function themechanger_custom_theme() {
	//echo "<h1>themechanger</h1>";
  // Allow the user to request a particular theme via a query parameter.
  if (isset($_GET['theme'])) {
    return $_GET['theme'];
	//return "Bartik";
  }
}

